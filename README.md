# Svelte + Vite + Docker + Nginx

## How install

```bash
git clone https://github.com/BruhaBruh/svelte-docker-todoapp.git
```

## How run docker

```bash
docker compose up -d
```

## After open http://localhost to see result
